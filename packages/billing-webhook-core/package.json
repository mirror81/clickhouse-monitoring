{
  "name": "@chm/billing-webhook-core",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Framework-agnostic Polar webhook core: applySubscription flow + D1 subscription-store contract, shared by apps/dashboard and apps/cloud-hooks so billing behaviour cannot fork.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test src/",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@chm/pricing": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  },
  "files": [
    "src",
    "tsconfig.json"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
